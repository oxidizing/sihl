(executables
 (names csrf csrf_mariadb http)
 (libraries sihl-user sihl-web sihl-persistence alcotest-lwt
   caqti-driver-mariadb))

(rule
 (alias runtest-all)
 (action
  (run ./csrf_mariadb.exe)))

(rule
 (alias runtest-all)
 (action
  (run ./http.exe)))
